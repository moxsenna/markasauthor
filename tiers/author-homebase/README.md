# Author Homebase (hero demo)

```bash
npm install
npm run dev   # http://localhost:4322
```

Routes: `/`, `/bonus`, `/bonus/terima-kasih`, `/peta-baca`, `/kolaborasi`, `/karya`, `/karya/[slug]`, `/dashboard`, `/untuk-penulis`

Persona: Ratna Maharani. Mock only -- localStorage, no real backend.
