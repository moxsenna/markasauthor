# Signature Universe

```bash
npm install
npm run dev   # http://localhost:4323
```

Routes: `/`, `/bonus`, `/bonus/terima-kasih`, `/peta-baca`, `/kolaborasi`, `/karya`, `/karya/[slug]`, `/dashboard`, `/untuk-penulis`, `/universe`, `/universe/tokoh`, `/universe/tokoh/[slug]`, `/universe/timeline`, `/universe/peta`, `/universe/trivia`

Persona: Ratna Maharani. Mock only -- localStorage, no real backend.
